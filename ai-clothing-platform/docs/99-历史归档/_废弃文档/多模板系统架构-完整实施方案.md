# 🎨 多模板提示词系统 - 完整实施方案

## 📅 文档版本：v1.0

## 📅 更新日期：2026-01-29

---

## 🎯 概述

本文档提供**多模板提示词系统**的完整实施方案，支持：

- ✅ 多个提示词模板共存
- ✅ 用户根据场景选择模板
- ✅ 动态表单根据模板切换
- ✅ 无限扩展新模板
- ✅ 避免模板间冲突

---

## 📊 系统架构

### 核心设计理念

```
用户选择模板 → 根据模板动态生成表单 → 后端组装提示词 → N8N生成
```

### 完整数据流

```
┌─────────────────────────────────────────────────────────────┐
│                    前端Web界面                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 步骤1：选择提示词模板                               │   │
│  │ ◉ 服装模特展示（适合服装、配饰）                  │   │
│  │ ○ 产品静物展示（适合家居、器皿、电子产品）        │   │
│  │ ○ 分层逻辑控制（适合专业摄影师、高级用户）       │   │
│  │ ○ 自定义（完全自定义提示词）                      │   │
│  └─────────────────────────────────────────────────────┘   │
│         ↓                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 步骤2：根据选择的模板显示对应表单                  │   │
│  │                                                       │   │
│  │ 如果选择"服装模特展示"：                             │   │
│  │   - 产品描述: [__________]                          │   │
│  │   - 模特类型: [下拉选择]                            │   │
│  │   - 场景风格: [下拉选择]                            │   │
│  │                                                       │   │
│  │ 如果选择"产品静物展示"：                             │   │
│  │   - 产品描述: [__________]                          │   │
│  │   - 材质: [下拉选择]                                │   │
│  │   - 空间位置: [下拉选择]                            │   │
│  │   - 光线方向: [下拉选择]                            │   │
│  │   - 镜头类型: [下拉选择]                            │   │
│  │                                                       │   │
│  │ 如果选择"分层逻辑控制"：                             │   │
│  │   - 物理层（材质、体积、颜色）                       │   │
│  │   - 空间层（位置、环境、背景）                       │   │
│  │   - 光影层（方向、色调、质量）                       │   │
│  │   - 感知层（镜头、景深、焦点）                       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                    后端API                                 │
│  根据选择的模板和填写的参数，调用对应的提示词构建函数       │
│         ↓                                                   │
│  生成完整的结构化提示词                                    │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                    N8N工作流（无需改动）                      │
│  接收完整提示词 → AI图片生成 → 返回结果                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 模板配置系统

### 1. 模板配置文件

**文件**：`src/lib/prompts/templates/index.ts`

```typescript
/**
 * 提示词模板配置
 * 集中管理所有可用的提示词模板
 */

export interface TemplateConfig {
  id: string;
  name: string;
  description: string;
  icon: string;
  useCases: string[];
  formComponent: string;
  buildFunction: string;
  category: 'fashion' | 'product' | 'advanced' | 'custom';
}

export const TEMPLATE_TYPES: TemplateConfig[] = [
  {
    id: 'fashion_model',
    name: '服装模特展示',
    description: '将白底产品图转化为真人模特场景展示图',
    icon: '👔',
    useCases: ['服装', '配饰', '鞋包', '时尚'],
    formComponent: 'FashionModelForm',
    buildFunction: 'buildFashionModelPrompt',
    category: 'fashion',
  },
  {
    id: 'product_still',
    name: '产品静物展示',
    description: '通过分层逻辑精准控制产品静物展示效果',
    icon: '🏺',
    useCases: ['家居', '器皿', '电子产品', '化妆品'],
    formComponent: 'ProductStillForm',
    buildFunction: 'buildLayeredLogicPrompt',
    category: 'product',
  },
  {
    id: 'layered_logic',
    name: '分层逻辑控制',
    description: '专业级分层控制，适合高级用户',
    icon: '🎯',
    useCases: ['专业摄影', '艺术创作', '技术教学'],
    formComponent: 'LayeredLogicForm',
    buildFunction: 'buildLayeredLogicPrompt',
    category: 'advanced',
  },
  {
    id: 'custom',
    name: '自定义提示词',
    description: '完全自定义提示词，适合高级用户',
    icon: '✏️',
    useCases: ['特殊场景', '实验性创作'],
    formComponent: 'CustomPromptForm',
    buildFunction: 'buildCustomPrompt',
    category: 'custom',
  },
];

// 模板分类
export const TEMPLATE_CATEGORIES = {
  fashion: {
    name: '时尚服装',
    icon: '👔',
    description: '适合服装、配饰、鞋包等时尚产品',
  },
  product: {
    name: '产品静物',
    icon: '🏺',
    description: '适合家居、器皿、电子产品等',
  },
  advanced: {
    name: '高级控制',
    icon: '🎯',
    description: '适合专业摄影师和高级用户',
  },
  custom: {
    name: '自定义',
    icon: '✏️',
    description: '完全自定义提示词',
  },
};
```

### 2. 模板选择器组件

**文件**：`src/components/prompts/TemplateSelector.tsx`

```typescript
'use client';

import { useState } from 'react';
import { TEMPLATE_TYPES, TEMPLATE_CATEGORIES } from '@/lib/prompts/templates';
import { FashionModelForm } from './FashionModelForm';
import { ProductStillForm } from './ProductStillForm';
import { LayeredLogicForm } from './LayeredLogicForm';
import { CustomPromptForm } from './CustomPromptForm';

interface TemplateSelectorProps {
  onPromptGenerated: (prompt: string, metadata: any) => void;
}

export function TemplateSelector({ onPromptGenerated }: TemplateSelectorProps) {
  const [selectedTemplate, setSelectedTemplate] = useState<string>('fashion_model');
  const [selectedCategory, setSelectedCategory] = useState<string>('all');

  // 根据分类筛选模板
  const filteredTemplates = selectedCategory === 'all'
    ? TEMPLATE_TYPES
    : TEMPLATE_TYPES.filter(t => t.category === selectedCategory);

  return (
    <div className="space-y-6">
      {/* 分类筛选 */}
      <div className="flex gap-2 flex-wrap">
        <button
          onClick={() => setSelectedCategory('all')}
          className={`px-4 py-2 rounded-lg text-sm ${
            selectedCategory === 'all'
              ? 'bg-primary text-primary-foreground'
              : 'bg-gray-100 hover:bg-gray-200'
          }`}
        >
          全部模板
        </button>
        {Object.entries(TEMPLATE_CATEGORIES).map(([key, category]) => (
          <button
            key={key}
            onClick={() => setSelectedCategory(key)}
            className={`px-4 py-2 rounded-lg text-sm ${
              selectedCategory === key
                ? 'bg-primary text-primary-foreground'
                : 'bg-gray-100 hover:bg-gray-200'
            }`}
          >
            {category.icon} {category.name}
          </button>
        ))}
      </div>

      {/* 模板列表 */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {filteredTemplates.map((template) => (
          <button
            key={template.id}
            onClick={() => setSelectedTemplate(template.id)}
            className={`p-4 text-left border-2 rounded-lg transition-all ${
              selectedTemplate === template.id
                ? 'border-primary bg-primary/5 shadow-sm'
                : 'border-gray-200 hover:border-gray-300'
            }`}
          >
            <div className="flex items-start gap-3">
              <span className="text-2xl">{template.icon}</span>
              <div className="flex-1">
                <div className="font-medium">{template.name}</div>
                <div className="text-xs text-muted-foreground mt-1">
                  {template.description}
                </div>
                <div className="text-xs text-muted-foreground mt-2">
                  适用场景：{template.useCases.join('、')}
                </div>
              </div>
            </div>
          </button>
        ))}
      </div>

      {/* 动态表单 */}
      <div className="border-t pt-6">
        <h3 className="font-medium mb-4">填写参数</h3>
        <TemplateForm
          templateId={selectedTemplate}
          onPromptGenerated={onPromptGenerated}
        />
      </div>
    </div>
  );
}

// 动态表单组件
function TemplateForm({
  templateId,
  onPromptGenerated,
}: {
  templateId: string;
  onPromptGenerated: (prompt: string, metadata: any) => void;
}) {
  switch (templateId) {
    case 'fashion_model':
      return <FashionModelForm onPromptGenerated={onPromptGenerated} />;
    case 'product_still':
      return <ProductStillForm onPromptGenerated={onPromptGenerated} />;
    case 'layered_logic':
      return <LayeredLogicForm onPromptGenerated={onPromptGenerated} />;
    case 'custom':
      return <CustomPromptForm onPromptGenerated={onPromptGenerated} />;
    default:
      return <FashionModelForm onPromptGenerated={onPromptGenerated} />;
  }
}
```

---

## 🎨 各模板的表单组件

### 1. 服装模特展示表单

**文件**：`src/components/prompts/FashionModelForm.tsx`

```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { buildFashionModelPrompt } from '@/lib/prompts/templates/fashion-model.template';

interface FashionModelFormProps {
  onPromptGenerated: (prompt: string, metadata: any) => void;
}

export function FashionModelForm({ onPromptGenerated }: FashionModelFormProps) {
  const [productDescription, setProductDescription] = useState('');
  const [modelType, setModelType] = useState<string>('');
  const [sceneStyle, setSceneStyle] = useState<string>('');

  const handleSubmit = () => {
    const prompt = buildFashionModelPrompt({
      productDescription,
      modelType: modelType || undefined,
      sceneStyle: sceneStyle || undefined,
    });

    onPromptGenerated(prompt, {
      templateId: 'fashion_model',
      productDescription,
      modelType,
      sceneStyle,
    });
  };

  return (
    <div className="space-y-4">
      {/* 产品描述 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          产品描述 <span className="text-red-500">*</span>
        </label>
        <Input
          placeholder="例如：红色连衣裙、蓝色西装、白色衬衫..."
          value={productDescription}
          onChange={(e) => setProductDescription(e.target.value)}
        />
        <p className="text-xs text-muted-foreground">
          简单描述你的产品，系统会自动生成专业的商业摄影提示词
        </p>
      </div>

      {/* 模特类型 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          模特类型 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={modelType} onValueChange={setModelType}>
          <SelectTrigger>
            <SelectValue placeholder="默认（亚洲模特）" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">默认（亚洲模特）</SelectItem>
            <SelectItem value="young_asian_female">年轻亚洲女性</SelectItem>
            <SelectItem value="young_asian_male">年轻亚洲男性</SelectItem>
            <SelectItem value="western_female">欧美女性</SelectItem>
            <SelectItem value="western_male">欧美男性</SelectItem>
            <SelectItem value="diverse">多元化模特</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 场景风格 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          场景风格 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={sceneStyle} onValueChange={setSceneStyle}>
          <SelectTrigger>
            <SelectValue placeholder="默认（极简高级环境）" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">默认（极简高级环境）</SelectItem>
            <SelectItem value="cozy_bedroom">温馨卧室</SelectItem>
            <SelectItem value="modern_living">现代客厅</SelectItem>
            <SelectItem value="urban_street">城市街头</SelectItem>
            <SelectItem value="minimal_studio">极简摄影棚</SelectItem>
            <SelectItem value="nature_outdoor">自然户外</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 生成按钮 */}
      <Button
        onClick={handleSubmit}
        disabled={!productDescription}
        className="w-full"
      >
        生成提示词
      </Button>
    </div>
  );
}
```

### 2. 产品静物展示表单

**文件**：`src/components/prompts/ProductStillForm.tsx`

```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { buildLayeredLogicPrompt } from '@/lib/prompts/templates/layered-logic.template';

interface ProductStillFormProps {
  onPromptGenerated: (prompt: string, metadata: any) => void;
}

export function ProductStillForm({ onPromptGenerated }: ProductStillFormProps) {
  const [productDescription, setProductDescription] = useState('');
  const [material, setMaterial] = useState('');
  const [position, setPosition] = useState('');
  const [lightDirection, setLightDirection] = useState('');
  const [lens, setLens] = useState('');

  const handleSubmit = () => {
    const prompt = buildLayeredLogicPrompt({
      physicsLayer: {
        material: material || '根据产品图片推断',
        color: productDescription,
      },
      spaceLayer: {
        position: position || '台面',
        environment: '室内',
      },
      lightLayer: {
        direction: lightDirection || '侧光',
        tone: '中性',
      },
      sensorLayer: {
        lens: lens || '85mm',
        depth: '浅景深',
        focus: '产品中心',
      },
    });

    onPromptGenerated(prompt, {
      templateId: 'product_still',
      productDescription,
      material,
      position,
      lightDirection,
      lens,
    });
  };

  return (
    <div className="space-y-4">
      {/* 产品描述 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          产品描述 <span className="text-red-500">*</span>
        </label>
        <Input
          placeholder="例如：陶瓷花瓶、玻璃杯、金属摆件..."
          value={productDescription}
          onChange={(e) => setProductDescription(e.target.value)}
        />
      </div>

      {/* 材质 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          材质 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={material} onValueChange={setMaterial}>
          <SelectTrigger>
            <SelectValue placeholder="自动识别" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">自动识别</SelectItem>
            <SelectItem value="陶瓷">陶瓷</SelectItem>
            <SelectItem value="金属">金属</SelectItem>
            <SelectItem value="木材">木材</SelectItem>
            <SelectItem value="塑料">塑料</SelectItem>
            <SelectItem value="玻璃">玻璃</SelectItem>
            <SelectItem value="石材">石材</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 空间位置 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          空间位置 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={position} onValueChange={setPosition}>
          <SelectTrigger>
            <SelectValue placeholder="默认（台面）" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">默认（台面）</SelectItem>
            <SelectItem value="水泥台面">水泥台面</SelectItem>
            <SelectItem value="木桌">木桌</SelectItem>
            <SelectItem value="玻璃桌面">玻璃桌面</SelectItem>
            <SelectItem value="地面">地面</SelectItem>
            <SelectItem value="悬挂">悬挂</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 光线方向 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          光线方向 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={lightDirection} onValueChange={setLightDirection}>
          <SelectTrigger>
            <SelectValue placeholder="默认（侧光）" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">默认（侧光）</SelectItem>
            <SelectItem value="side">侧光</SelectItem>
            <SelectItem value="back">逆光</SelectItem>
            <SelectItem value="top">顶光</SelectItem>
            <SelectItem value="front">顺光</SelectItem>
            <SelectItem value="natural">自然光</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 镜头类型 */}
      <div className="space-y-2">
        <label className="text-sm font-medium">
          镜头类型 <span className="text-muted-foreground">(可选)</span>
        </label>
        <Select value={lens} onValueChange={setLens}>
          <SelectTrigger>
            <SelectValue placeholder="默认（85mm人像）" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="">默认（85mm人像）</SelectItem>
            <SelectItem value="50mm">50mm（标准）</SelectItem>
            <SelectItem value="35mm">35mm（广角）</SelectItem>
            <SelectItem value="100mm">100mm（微距）</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 生成按钮 */}
      <Button
        onClick={handleSubmit}
        disabled={!productDescription}
        className="w-full"
      >
        生成提示词
      </Button>
    </div>
  );
}
```

### 3. 分层逻辑控制表单

**文件**：`src/components/prompts/LayeredLogicForm.tsx`

这个组件在前面已经详细展示过了，提供4个层级（物理层、空间层、光影层、感知层）的完整控制。

### 4. 自定义提示词表单

**文件**：`src/components/prompts/CustomPromptForm.tsx`

```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Textarea } from '@/components/ui/textarea';
import { Label } from '@/components/ui/label';

interface CustomPromptFormProps {
  onPromptGenerated: (prompt: string, metadata: any) => void;
}

export function CustomPromptForm({ onPromptGenerated }: CustomPromptFormProps) {
  const [customPrompt, setCustomPrompt] = useState('');

  const handleSubmit = () => {
    onPromptGenerated(customPrompt, {
      templateId: 'custom',
      prompt: customPrompt,
    });
  };

  return (
    <div className="space-y-4">
      <div className="space-y-2">
        <Label htmlFor="custom-prompt">
          自定义提示词 <span className="text-red-500">*</span>
        </Label>
        <Textarea
          id="custom-prompt"
          placeholder="输入你的自定义提示词..."
          value={customPrompt}
          onChange={(e) => setCustomPrompt(e.target.value)}
          rows={10}
          className="font-mono text-sm"
        />
        <p className="text-xs text-muted-foreground">
          完全自定义提示词，适合有特殊需求的用户
        </p>
      </div>

      <Button
        onClick={handleSubmit}
        disabled={!customPrompt}
        className="w-full"
      >
        使用自定义提示词
      </Button>
    </div>
  );
}
```

---

## 🎯 如何添加新模板

### 步骤1：在配置文件中添加模板定义

**文件**：`src/lib/prompts/templates/index.ts`

```typescript
export const TEMPLATE_TYPES: TemplateConfig[] = [
  // ... 现有模板

  // ✅ 新增模板：美食摄影
  {
    id: 'food_photography',
    name: '美食摄影',
    description: '专业的美食摄影提示词模板',
    icon: '🍽️',
    useCases: ['餐饮', '食品', '饮料', '甜点'],
    formComponent: 'FoodPhotographyForm',
    buildFunction: 'buildFoodPhotographyPrompt',
    category: 'product',
  },

  // ✅ 新增模板：汽车摄影
  {
    id: 'automotive',
    name: '汽车摄影',
    description: '汽车及交通工具摄影提示词模板',
    icon: '🚗',
    useCases: ['汽车', '摩托车', '自行车'],
    formComponent: 'AutomotiveForm',
    buildFunction: 'buildAutomotivePrompt',
    category: 'product',
  },
];
```

### 步骤2：创建对应的表单组件

**文件**：`src/components/prompts/FoodPhotographyForm.tsx`

```typescript
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Select } from '@/components/ui/select';
import { buildFoodPhotographyPrompt } from '@/lib/prompts/templates/food-photography.template';

export function FoodPhotographyForm({ onPromptGenerated }: any) {
  // 美食摄影特有的表单项
  const [dishName, setDishName] = useState('');
  const [platingStyle, setPlatingStyle] = useState('');
  const [angle, setAngle] = useState('');
  const [lighting, setLighting] = useState('');

  const handleSubmit = () => {
    const prompt = buildFoodPhotographyPrompt({
      dishName,
      platingStyle,
      angle,
      lighting,
    });

    onPromptGenerated(prompt, {
      templateId: 'food_photography',
      dishName,
      platingStyle,
      angle,
      lighting,
    });
  };

  return (
    <div className="space-y-4">
      <Input
        placeholder="菜品名称"
        value={dishName}
        onChange={(e) => setDishName(e.target.value)}
      />
      <Select value={platingStyle} onValueChange={setPlatingStyle}>
        {/* ... */}
      </Select>
      {/* ... 其他表单项 */}
      <Button onClick={handleSubmit} className="w-full">
        生成提示词
      </Button>
    </div>
  );
}
```

### 步骤3：创建对应的提示词构建函数

**文件**：`src/lib/prompts/templates/food-photography.template.ts`

```typescript
/**
 * 美食摄影提示词模板
 */

export interface FoodPhotographyParams {
  dishName: string;
  platingStyle?: string;
  angle?: string;
  lighting?: string;
}

export function buildFoodPhotographyPrompt(params: FoodPhotographyParams): string {
  const { dishName, platingStyle, angle, lighting } = params;

  return `# 美食摄影提示词

## 菜品信息
- 菜品名称：${dishName}
- 摆盘风格：${platingStyle || '精致摆盘'}
- 拍摄角度：${angle || '45度俯视'}
- 光线类型：${lighting || '自然侧光'}

## 生成指令

Professional food photography, ${dishName}, ${platingStyle || 'elegant plating'}, ${angle || '45-degree angle'}, ${lighting || 'natural side lighting'}, appetizing presentation, vibrant colors, sharp focus, shallow depth of field, commercial food photography, restaurant quality, 8k resolution, delicious, mouth-watering.

请严格按照以上指令生成高质量的美食摄影图片。`;
}
```

### 步骤4：在TemplateSelector中注册新表单

**文件**：`src/components/prompts/TemplateSelector.tsx`

```typescript
import { FoodPhotographyForm } from './FoodPhotographyForm';

// 在 TemplateForm 组件中添加新的 case
function TemplateForm({ templateId, onPromptGenerated }: any) {
  switch (templateId) {
    // ... 现有 case
    case 'food_photography':
      return <FoodPhotographyForm onPromptGenerated={onPromptGenerated} />;
    // ...
  }
}
```

**完成！** 新模板已经集成到系统中。

---

## 📊 如何避免模板冲突

### 冲突场景分析

```
模板A（服装模特）  →  输出：人物 + 场景 提示词
模板B（产品静物）  →  输出：产品 + 环境 提示词
```

**问题**：如果用户同时选择两个模板？

### 解决方案1：互斥选择（推荐）⭐⭐⭐⭐⭐

```tsx
// 用户只能选择一个模板
const [selectedTemplate, setSelectedTemplate] = useState('fashion_model');

// 界面上使用单选按钮，而不是复选框
<RadioGroup value={selectedTemplate} onValueChange={setSelectedTemplate}>
  <RadioGroupItem value="fashion_model">服装模特展示</RadioGroupItem>
  <RadioGroupItem value="product_still">产品静物展示</RadioGroupItem>
  <RadioGroupItem value="layered_logic">分层逻辑控制</RadioGroupItem>
</RadioGroup>;
```

**优势**：

- ✅ 界面简洁，逻辑清晰
- ✅ 避免混淆
- ✅ 易于实现

### 解决方案2：主模板 + 辅助增强（灵活性）⭐⭐⭐⭐

```tsx
// 用户选择一个主模板，然后选择辅助增强
const [mainTemplate, setMainTemplate] = useState('fashion_model');
const [enhancements, setEnhancements] = useState<string[]>([]);

// 辅助增强选项
const ENHANCEMENT_OPTIONS = [
  { id: 'layered_logic', name: '分层逻辑控制', description: '添加物理/空间/光影/感知层级' },
  { id: 'professional_lighting', name: '专业布光', description: '添加专业布光指令' },
  { id: 'color_grading', name: '色彩调色', description: '添加色彩调色指令' },
];

// 提示词组装
const basePrompt = buildTemplatePrompt(mainTemplate, params);
const enhancedPrompt = applyEnhancements(basePrompt, enhancements);
```

**优势**：

- ✅ 灵活性高
- ✅ 可以组合使用不同模板的优点
- ✅ 高级用户可以精细化控制

### 解决方案3：模板链（高级）⭐⭐⭐

```tsx
// 按顺序应用多个模板
const templateChain = ['fashion_model', 'layered_logic', 'professional_lighting'];

const prompt = templateChain.reduce((acc, templateId) => {
  return applyTemplate(acc, templateId);
}, initialInput);
```

**优势**：

- ✅ 最大灵活性
- ✅ 可以精细化控制每个环节
- ✅ 适合高级用户

---

## 📝 完整实施清单

### 第一步：创建模板配置系统（1小时）

- [ ] 创建 `src/lib/prompts/templates/index.ts`
- [ ] 定义模板配置结构
- [ ] 配置初始模板（4个）

### 第二步：创建模板选择器组件（2小时）

- [ ] 创建 `src/components/prompts/TemplateSelector.tsx`
- [ ] 实现模板列表UI
- [ ] 实现分类筛选功能

### 第三步：创建各模板的表单组件（4小时）

- [ ] 创建 `FashionModelForm.tsx`
- [ ] 创建 `ProductStillForm.tsx`
- [ ] 创建 `LayeredLogicForm.tsx`
- [ ] 创建 `CustomPromptForm.tsx`

### 第四步：创建提示词构建函数（3小时）

- [ ] 创建 `fashion-model.template.ts`
- [ ] 创建 `layered-logic.template.ts`
- [ ] 创建其他模板的构建函数

### 第五步：集成到主界面（2小时）

- [ ] 在 `ParamsPanel.tsx` 中集成模板选择器
- [ ] 实现提示词生成逻辑
- [ ] 测试整体流程

**总工作量**：约12小时

---

## 🎯 总结与建议

### 我的推荐：方案A（模板选择 + 动态表单）

**理由**：

1. ✅ **最灵活**：支持无限扩展
2. ✅ **最易用**：用户只需选择模板
3. ✅ **最易维护**：新增模板很方便
4. ✅ **避免冲突**：互斥选择，逻辑清晰

### 实施优先级：

**第一阶段**（核心功能，3-5天）：

- ✅ 实现3个核心模板（服装模特、产品静物、分层逻辑）
- ✅ 实现模板选择器和动态表单
- ✅ 集成到主界面

**第二阶段**（增强功能，2-3天）：

- 🔄 添加模板分类筛选
- 🔄 添加模板使用统计
- 🔄 优化表单验证

**第三阶段**（高级功能，按需）：

- 💡 实现模板混合功能
- 💡 实现模板推荐功能
- 💡 实现自定义模板保存

### 关于两个提示词的兼容性：

**第一个提示词**（服装模特展示）：

- 专注于人物 + 场景
- 适合：服装、配饰、鞋包
- 模板ID：`fashion_model`

**第二个提示词**（分层逻辑控制）：

- 专注于分层逻辑
- 适合：产品静物、专业摄影
- 模板ID：`layered_logic` 或 `product_still`

**结论**：两者**互补**，不冲突！都是很好的模板，可以并存。

---

您觉得这个方案如何？我们可以先实施第一阶段，看看效果，然后根据反馈继续优化。如果您有其他想法或建议，请告诉我！
